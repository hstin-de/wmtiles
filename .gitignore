*.grib2
*.wmt
./data/*.grib2
./data/*.wmt

# JS/TS workspace
node_modules/
*.log

# Build artifacts: regenerated locally and in CI; never committed.
wmtiles-js/dist/
wmtiles-js/.dist.stamp
cmd/wmtiles/web/dist/

# Test fixtures regenerated by `go run ./cmd/gen-testdata` before tests run.
format/testdata/

# Local binary outputs from `go build`.
/wmtiles
